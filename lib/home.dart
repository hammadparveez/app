import 'package:flutter/material.dart';

class MyDownloadWidget extends StatelessWidget {
  const MyDownloadWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
