import 'package:flutter/material.dart';

class MyWidget extends StatelessWidget {
  const MyWidget({super.key});
  //But Download is easy
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
